[project]
name = "monofx-pipeline-common"
version = "0.1.0"
description = "DCC-agnostic MonoFX pipeline helpers"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
